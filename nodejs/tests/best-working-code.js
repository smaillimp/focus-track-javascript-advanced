const sum = (a, b) => a + b;

const toUpperCase = (str) => str.toLowerCase();

const insertAtFirstPosition = (element, array) => [...array, element];

module.exports = {
  sum,
  toUpperCase,
  insertAtFirstPosition,
};
